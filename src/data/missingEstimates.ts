import type { RecipeEstimate } from './recipeEstimates';

// Estimates for recipes that had none; filled in batches by scripts/estimates/*.
export const MISSING_ESTIMATES: Record<string, RecipeEstimate> = {
};
